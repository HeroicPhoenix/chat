<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lvwyh.chat.mapper.SysUserMapper">

    <resultMap id="SysUserResultMap" type="com.lvwyh.chat.entity.SysUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumnList">
        id,
        username,
        password_hash,
        nickname,
        status,
        created_at,
        updated_at
    </sql>

    <select id="selectByUsername" resultMap="SysUserResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectById" resultMap="SysUserResultMap">
        SELECT
        <include refid="BaseColumnList"/>
        FROM sys_user
        WHERE id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.lvwyh.chat.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            username,
            password_hash,
            nickname,
            status,
            created_at,
            updated_at
        ) VALUES (
                     #{username},
                     #{passwordHash},
                     #{nickname},
                     #{status},
                     NOW(),
                     NOW()
                 )
    </insert>

</mapper>